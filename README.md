# 在 windows 环境下开发，其他应该用不了。

# 配置

你需要用到：python，requests、bs4 库。

下载整个 files 文件夹，放在什么目录下边无所谓。

选择你需要 x32 还是 x64 的 exe。

# 运行

## 请全程在 cmd 中运行，否则奇奇怪怪的问题将不保证。

首先打开你配置的目录的 cmd，具体来说你可以打开文件夹，点击上面的目录栏，直接键入 cmd。

process 用法：（以process64 为例）

process（默认进程数、默认浏览数、默认网站（五彩斑斓那个帖子））

process 进程数（默认浏览数、默认网站）

process 进程数 单进程浏览数（默认网站）

process 进程数 单进程浏览数 网站名

**输入奇怪的东西将不会保证。**

一个例子：```process 5 600 https://xn--fx-ex2c330n.ml/show.php?url=https://www.luogu.com.cn/discuss/423386```

monitor 用法：

### 注意：只用于 https://fx白丝.ml

## 注意：只用于 https://fx白丝.ml

# 注意：只用于 https://fx白丝.ml

moniter （查看默认网站的浏览数）

moniter 网站名

如果运行的时候后台还有 process 的子进程将会返回一个错误信息。

shut.bat 用法：

显然的，shut。这会直接杀死后台的处理进程，没什么危险但最好不要老是点点点的。

# 有什么问题洛谷私信作者。
